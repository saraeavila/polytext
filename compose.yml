services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: polytext
      POSTGRES_USER: polytext
      POSTGRES_PASSWORD: polytext
    ports:
      - "5432:5432"
    volumes:
      - polytext_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U polytext -d polytext"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  polytext_postgres_data:
